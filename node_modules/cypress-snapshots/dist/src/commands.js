"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addCypressSnapshotsCommands = void 0;
function addCypressSnapshotsCommands() {
    Cypress.Commands.add('expectSnapshot', { prevSubject: 'optional' }, function ($subject, name) {
        var args = {
            name: name,
        };
        // Take a screenshot with specific name
        return ($subject ? cy.wrap($subject) : cy)
            .screenshot("__snapshot__" + args.name)
            .then(function () {
            // Runs comparison of expected snapshot and actual screenshot.
            cy.task('runSnapshotComparison', args);
        });
    });
}
exports.addCypressSnapshotsCommands = addCypressSnapshotsCommands;
